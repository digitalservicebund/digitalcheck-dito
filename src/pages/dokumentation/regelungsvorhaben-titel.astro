---
import Layout from "@/layouts/Layout.astro";
import { TitelPage } from "~/routes/dokumentation._documentationNavigation.regelungsvorhaben-titel";
import {
  fetchStrapiData,
  GET_PRINZIPS_WITH_EXAMPLES_QUERY,
} from "~/utils/strapiData.server";
import type { PrinzipWithAspekteAndExample } from "~/utils/strapiData.types";

export const frontmatter = {
  title: "Regelungsvorhaben Titel",
};

const result = await fetchStrapiData<{
  prinzips: PrinzipWithAspekteAndExample[];
}>(GET_PRINZIPS_WITH_EXAMPLES_QUERY);
const prinzips = "error" in result ? [] : result.prinzips;
---

<Layout title={frontmatter.title} showProgressBar>
  <TitelPage client:load prinzips={prinzips} currentUrl={Astro.url.pathname} />
</Layout>
