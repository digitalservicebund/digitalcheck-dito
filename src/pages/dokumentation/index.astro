---
import Layout from "@/layouts/Layout.astro";
import DokumentationIndexPage from "~/routes/dokumentation._index";
import {
  fetchStrapiData,
  GET_PRINZIPS_WITH_EXAMPLES_QUERY,
} from "~/utils/strapiData.server";
import type { PrinzipWithAspekteAndExample } from "~/utils/strapiData.types";

export const frontmatter = {
  title: "Dokumentation",
};

const result = await fetchStrapiData<{
  prinzips: PrinzipWithAspekteAndExample[];
}>(GET_PRINZIPS_WITH_EXAMPLES_QUERY);
const prinzips = "error" in result ? [] : result.prinzips;
---

<Layout title={frontmatter.title}>
  <DokumentationIndexPage client:only="react" prinzips={prinzips} />
</Layout>
