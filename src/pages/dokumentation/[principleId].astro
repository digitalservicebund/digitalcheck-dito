---
import { getPrincipleIdStaticPaths } from "~/utils/strapiData.server";
import DocumentationWizard from "@/components/DocumentationWizard.astro";
export const getStaticPaths = getPrincipleIdStaticPaths;

// props are generated by getStaticPaths
const { principleName } = Astro.props;
const { principleId } = Astro.params;
---

<DocumentationWizard stepTitle={principleName} principleId={principleId} />
