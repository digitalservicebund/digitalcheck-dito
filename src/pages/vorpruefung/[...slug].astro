---
import Layout from "@/layouts/Layout.astro";
import VorpruefungApp from "~/components/VorpruefungApp";

export function getStaticPaths() {
  return [
    { params: { slug: "it-system" } },
    { params: { slug: "verpflichtungen-fuer-beteiligte" } },
    { params: { slug: "datenaustausch" } },
    { params: { slug: "kommunikation" } },
    { params: { slug: "automatisierung" } },
    { params: { slug: "eu-bezug" } },
    { params: { slug: "hinweise" } },
    { params: { slug: "ergebnis" } },
  ];
}
---

<Layout showProgressBar={true}>
  <VorpruefungApp client:load />
</Layout>
